# CLAUDE.md — 短视频脚本系统 v4.0

## 系统定位

这是一个 **Claude Code 驱动的个人IP短视频生产系统**。

- **Claude 负责**：创意决策 — 理解素材语义、定中心思想、写脚本、联网搜索、分析反馈、长期战略规划
- **Python 负责**：数据计算 — 策略决策（3:4:3 支柱轮换）、记录存储、反馈分析、洞察生成

**终极目标**：打造个人IP"佳佳 = 上海改善置换专家"。主攻微信视频号 + 小红书。
**北极星指标**：不是粉丝量，是**优质客户转化**——从视频到加微信到最终成交。

## 长期战略：三阶段路线图

| 阶段 | 时间 | 目标 | 当前状态 |
|------|------|------|----------|
| 验证期 | 第1-8周 | 找到"哪种内容吸引对的人" | **← 当前** |
| 深耕期 | 第9-24周 | 把一个已验证模式做到极致 | 待进入 |
| 破圈期 | 第25周+ | 从算法推荐到社交推荐 | 待进入 |

**当前阶段重点**：
- 每周至少1条视频收到有效私信（真实问房的人，非同行点赞）——这是北极星指标
- 每5条做一次A/B测试（行家型 vs 避坑型 vs 陪伴型），找到最优风格组合
- 不发没有定位的视频——每条发布前确认"这条视频会让观众记住我什么"

**阶段切换判断**：当同时满足"有一条内容模式连续3条获得有效私信 + 对这个模式不再有选题焦虑 + 至少2-3个客户明确说看了视频才联系你"，可进入深耕期。

## 文件结构

```
短视频/
├── CLAUDE.md                         # 本文件 — Claude 操作手册
├── engine.py                         # ★ 策略引擎：status / record / analyze
├── viral_validator.py                # 9 维爆款验证器（27 分制）
├── pleasure_scorer.py                # 爽点评分器（12 分制）
├── douyin_extractor.py               # 抖音视频文案提取（基础+ASR）
├── video_editor.py                   # ★ 视频自动剪辑引擎（脚本+素材→成片）
├── config/
│   ├── persona.json                  # 人设配置（违禁词/偏好句式/金句库/价值观/IP定位）
│   └── video_format_matrix.json      # ★ 画面形式决策矩阵（4方向×3支柱→3格式）
├── data/
│   ├── history.json                  # 每条脚本的生成记录 + 发布数据
│   └── insights.json                 # 引擎自动生成的规律发现
├── memory/                           # ★ 长期记忆系统（四层）
│   ├── layer1-identity/              # 我是谁：定位、核心观点
│   ├── layer2-strategy/              # 我怎么打：内容模式、复盘、竞品、CTA策略
│   ├── layer3-execution/             # 我做了什么：文案技巧、钩子库、金句记录、拍摄指南、素材捕捉、制作运营、话题弹药库
│   │   ├── solo-shooting-guide.md    # ★ 单人拍摄实操指南（三级设备方案）
│   │   ├── material-source-guide.md  # ★ 素材来源指南（从日常工作中提取素材）
│   │   ├── inspiration-topics.md     # 话题弹药库（素材枯竭时的备用选题）
│   │   ├── production-playbook.md    # ★ 制作运营手册（封面/标题/字幕/剪辑/发布）
│   │   └── competitor-learning-log.md # ★ 竞品拆解学习日志（攒够再分析，不改技能文件）
│   └── layer4-feedback/              # 外界反应：客户咨询来源
└── .claude/                          # Claude Code 技能配置
    └── skills/
        ├── douyin-extract.md         # 抖音视频文案提取技能
        └── video-editor.md           # ★ 视频自动剪辑技能（4种模式）
```

## 人设铁律（每次生成必遵）

从 `config/persona.json` 加载，核心要点：

**违禁词（12 个）**：家人们、绝绝子、震惊、重磅、赶紧、手慢无、错过不再、必看、血赚、抢疯了、暴涨、暴跌

**偏好句式（8 个）**：我建议、我觉得、你可以考虑、不妨看看、如果不介意、说实话、说白了、其实就一点

**金句库**：
- 我不帮你做决定，我帮你看清选项。
- 好房子不是推销出来的，是筛选出来的。
- 说到底，房子是给人住的，合适比便宜重要。

**⚠ 视频 CTA 铁律**：视频里绝不流露"找我买房"的暗示。观众对中介高度敏感，一闻到推销味就划走。CTA 只引导**点赞、关注、收藏**——先把人设和粉丝做起来，转化在内容之外自然发生。详见 `memory/layer2-strategy/video-cta-strategy.md`。

**核心价值观**：不催单 / 帮客户筛选而非推销 / 用真实数据说话 / 承认不完美 / 站在客户立场

**语言风格**：朋友聊天式。像在跟朋友打电话，不是老师上课，不是中介推销。

## 每日工作流程

### 0. 战略对齐（每次生成前必做）
在开始写脚本之前，先读 `memory/layer1-identity/positioning.md` 确认当前定位。
问自己：今天这条视频发布后，观众会记住"佳佳 = 什么"？
如果这条视频换个其他中介来念也没区别 → 说明定位没体现 → 换个角度。

### 1. 获取方向与素材

用户可能提供素材（一段文字 / 一个链接 / 一个话题），也可能只说"生成今日脚本"。

**如果用户没给素材**，不要直接让他"去找素材"。先读 `memory/layer3-execution/material-source-guide.md`，按素材指南的框架引导他：
1. 最近有没有带看？客户说了什么？
2. 最近有没有刷到有意思的房源截图？
3. 最近有没有客户问过让你觉得"这个问题值得展开讲"的问题？
4. 最近有没有看到同行的内容让你觉得"我不太同意"？

根据用户的回答，从中提取可用的素材，再进入下一步。

如果以上引导都无法产出有效素材，读 `memory/layer3-execution/inspiration-topics.md`，从话题弹药库中选题，结合一条假设的客户场景作为引子进入下一步。

如果用户给了一个抖音链接，默认用 `python douyin_extractor.py <链接> --asr --comments` 提取完整口播文案 + 热门评论。评论区是判断视频为什么火的最佳来源，提取后先分析评论再写拆解。

**拆解后必须做的事**：将提取到的可复用技法**写入 `memory/layer3-execution/competitor-learning-log.md`**，不要直接修改任何技能文件（copywriting-techniques、hook-library、production-playbook 等）。技法积累到 10+ 条后，统一分析并决定如何纳入系统。

### 2. 跑引擎看状态
```bash
python engine.py status
```
输出今日策略、支柱、**画面形式推荐**（[格式] 面板）、近期表现、支柱分布、IP方向权重。
重点关注：策略决定了"说什么"，格式决定了"怎么拍"——两者配合才有好视频。

### 3. 分析素材 + 定中心思想（核心步骤）

拿到素材后，先做三件事：
1. **评估信息密度**：有数字吗？有冲突吗？有决策吗？有反转吗？
2. **决定目标观众**：这条视频想吸引谁？（资产配置型 / 改善自住型 / 首次入市型）
3. **锁定中心思想**：用一句话写出核心论点。必须是可争论的判断，不能是"买房要看户型"这类废话。

然后输出 **素材提炼结果**，格式如下：

```
## 素材提炼结果

**素材来源**：<客户原话 / 带看观察 / 数据截图 / 同行内容 / 其他>
**中心思想**：<一句话可争论的判断>
**目标观众**：资产配置型 / 改善自住型 / 首次入市型
**信息密度**：数字(有/无) / 冲突(有/无) / 决策点(有/无) / 反转(有/无)
**定位契合度**：高/中/低 — <一句话解释为什么契合佳佳=改善置换专家>
**人设安全性**：有利 / 中性 / 不利 — <一句话判断>
**画面可行性**：可实拍 / 可混剪 / 仅口播 — <理由>
**实物锚点**：<这条素材绑定的具体事物是什么？一套房？一个客户问题？一条成交数据？没有实物锚点则标记"无">
```

### 3.5 素材就绪检查（量化标准）

输出提炼结果后，先做**人设安全检查**，再对四项维度打分。

#### ⛔ 第一关：人设安全检查（一票否决）

**在数四项之前，先问自己**：这条素材发出去，观众对佳佳的信任是增加还是减少？

| 判定 | 标准 | 处理 |
|------|------|------|
| **有利** | 强化"专业、说实话、替客户着想"的印象 | 通过，进入四项打分 |
| **中性** | 不增不减，换个中介讲也没区别 | 提高警惕——中性素材很容易沦为无效内容 |
| **不利** | 会让观众质疑佳佳的能力/诚信/判断力 | **直接否决，不进入四项打分** |

**对人设不利的典型情况**（必须拦截）：
- 暴露自身弱点且无法转化为共情（如"我几个月没开单"、"我不懂这个板块"）
- 让观众产生"你行不行"的质疑（即使是间接暗示）
- 抱怨客户/市场/同事（显得不专业）
- 任何降低信任感的表述

**拦截后必须做的事**：不是简单说"这个不行"，而是给用户一个**换个角度就能用**的建议。同样的真实经历，落脚点不同，人设影响完全不同。

#### 第二关：五项维度打分

第一关通过后，对以下五项打分。**至少满足四项**才提交用户审批：

| 维度 | 达标标准 | 不达标 |
|------|----------|--------|
| **可争论性** | 中心思想是一个观点/判断，不是事实陈述 | "买房要看户型"、"上海房价很高" |
| **有具体信息** | 至少包含1个数字/地名/时间/人名 | 全是抽象概括 |
| **定位相关** | 观众看完会记住"佳佳=改善置换专家" | 换其他中介说也没区别 |
| **可拍摄** | 用户今天/明天有条件拍出画面 | 素材只有一句话且无配图无房源 |
| **有实物锚点** | 素材绑定了一个具体事物（某套房/某条成交数据/某句客户原话/某张截图），观点是围绕它展开的 | 只有抽象观点，没有具体的事物托底 |

> **实物锚点是"房子+观点"二合一模型的核心**：纯粹展示房子没有观点=平淡，纯粹讲道理没有实物=飘在空中。好内容是"具体的一套房子/一个问题/一条数据 + 你作为专业人士对它的判断"。

**示例**：
- 素材"客户问我天山和古北为什么差3万" → 人设安全 ✓ + 5/5 ✓ → 提交审批
- 素材"买房要注意什么" → 人设安全 ✓ + 1/5 ✗ → 请用户具体化
- 素材"我几个月没开单很迷茫" → 人设安全 ✗ → 直接拦截，建议换角度

### 3.5.5 ⛔ 审批关卡（必须）

素材提炼结果达标后，**必须等用户确认"可以，进入下一步"或修改中心思想后**，才能开始写脚本。

用户可能会：
- 说"可以" → 进入步骤 3.6
- 调整中心思想 → 更新提炼结果，再次确认
- 说这个角度不好 → 换角度重新提炼

**禁止在用户确认前写脚本。**

### 3.6 确定画面形式

用户确认素材后，确定画面形式。Step 2 的 `[格式]` 面板已给出引擎推荐，但需根据实际情况调整：

**口播 (talking_head)** — 需要确认：
- [ ] 今天能在一个安静、光线好的房间拍摄吗？
- [ ] 有没有提词器方案（手机分屏/iPad/打印纸贴在镜头下方）？
- [ ] 服装是否纯色、避免细条纹/密集格子？

**实拍/探盘 (property_walk)** — 需要确认：
- [ ] 今天是否有可进入的房源？（自拍或客户看房顺便拍）
- [ ] 房东/业主是否同意拍摄？
- [ ] 稳定器带了没有？
- 如果无法进入房源 → 用 `python engine.py status` 的结果，告知引擎 `no_venue=True`，降级为口播

**混剪 (mixed_montage)** — 需要确认：
- [ ] 有没有足够的素材图片？（数据截图/地图/房源照片 ≥ 5张）
- [ ] 安静房间录 VO 的环境准备好了吗？

**格式决策规则**（参考 `config/video_format_matrix.json` override_rules）：
1. 有房源可拍 → 强制实拍（Property 支柱天然适合现场）
2. 连续 3 条同一形式 → 强制切下一个
3. 天气/场地不可用 → 实拍降级口播
4. 仅有数据/观点素材 → 口播或混剪

确认后把最终选择的格式传给脚本生成流程。

### 4. 写脚本

脚本文件要求包含 **YAML frontmatter** + **脚本正文**。

结构要求：
```
---
date: YYYY-MM-DD
format: talking_head | property_walk | mixed_montage
pillar: Market | Property | Story
strategy: 陪伴决策 | 专业分析 | 避坑指南 | 资源推荐
bgm: <BGM风格描述>
scene: <拍摄场景>
shot: <机位描述>
word_count_target: <字数范围>
best_platform: <推荐发布平台>
---

# 标题（吸引眼球但不标题党）

## 开场（3秒钩子）
分享式发现，不是命令式警告
[画面:xxx]

## 反差 / 痛点 / 分析（根据策略调整）
每段只论证中心思想的一个侧面
[画面:xxx] [字幕叠加:xxx]

## 金句
从金句库选，或即兴写一条人设一致的
[画面:xxx] [语速:慢]

## 结尾（引导关注）
引导点赞关注，不做成交暗示。承诺后续内容价值，让观众有理由关注
[画面:xxx] [字幕叠加:关注引导]
```

**每段脚本必须标注**：
- `[画面:xxx]` — 这段口播时画面该展示什么
- `[字幕叠加:xxx]` — 关键词/数字/金句的屏幕文字叠加
- `[语速:快/正常/慢]` — 节奏变化点

字数：按 `video_format_matrix.json` 中各格式的 `script_word_count` 建议，口播 280-400 字，实拍 250-400 字（画面已传信息），混剪 200-350 字（卡片承载信息）。

**写法参考**：生成前读 `memory/layer3-execution/copywriting-techniques.md` 中的自查清单。

### 5. 跑验证器
```bash
python viral_validator.py <脚本文件路径>     # 9维爆款验证（27分制）
python pleasure_scorer.py <脚本文件路径>     # 爽点评分（12分制）
```
注意：验证器是机械评分，字数统计含 YAML/markdown 标记，实际口播字数通常比统计值少 40-50%。18 分以下不一定不合格——如果核心问题（钩子、中心思想）实际已到位，可以手动判断不盲目追分。

### 6. 人设自查

1. 对着违禁词清单扫一遍，确保 0 违禁词
2. 检查语气是不是朋友聊天式
3. 对照 `config/persona.json` 中的 `emotional_lever_rules`，逐段过一遍：
   - 有没有在预测房价走势？（→ 删）
   - 有没有在攻击同行人格？（→ 改写成质疑观点）
   - 有没有在制造时间压迫感？（→ 改成揭示隐藏风险）
4. 跑一遍 `smell_test`：一个拿着 1000 万预算的客户读到每句话，会觉得"这人在帮我分析"还是"这人在吓我/催我"？

### 7. 记录 + 输出
跑 `python engine.py record` 或直接更新 `data/history.json`。
脚本保存到当前用户桌面：`$env:USERPROFILE\Desktop\daily-script-YYYY-MM-DD.md`

### 8. 更新记忆（每次生成后）
- 如果今天用了新钩子且效果好，追加到 `memory/layer3-execution/hook-library.md`
- 如果用了金句，更新 `memory/layer3-execution/golden-sentence-log.md` 的使用次数
- 如果产生了一个新的核心观点，添加到 `memory/layer1-identity/core-thesis.md`
- CTA 策略务必遵循 `memory/layer2-strategy/video-cta-strategy.md`：视频内只引导互动，不引导成交

### 8.5 输出制作运营建议（新增步骤）

脚本生成完毕后，读 `memory/layer3-execution/production-playbook.md`，输出制作运营指南：

1. **封面建议**：参考 playbook 第一章的封面文字公式和布局规则
2. **标题备选**：基于 playbook 标题公式库，给出 3 个标题备选
3. **剪辑要点**：根据当天画面形式，给出分段剪辑节奏建议
4. **发布时间**：当天最优发布时段建议
5. **标签建议**：双平台标签推荐

### 8.6 输出拍摄清单

脚本生成完毕后，输出一份可直接使用的拍摄清单，参考 `memory/layer3-execution/solo-shooting-guide.md`：

```
## 今日拍摄清单

**画面形式**：口播 / 实拍 / 混剪
**场景**：<具体拍摄地点>
**服装**：<推荐颜色/款式>
**设备**：<需携带的设备列表>
**BGM**：<风格 + 关键词，方便在剪映搜索>
**提词器**：<手机分屏 / iPad / 打印纸>
**时长预估**：拍摄 __ 分钟 + 剪辑 __ 分钟

**分段画面指导**：
| 段落 | 时间 | 画面 | 字幕叠加 | 语速 |
|------|------|------|----------|------|
| 开场 | 0-3s | ... | ... | 正常 |
| 分析 | 3-20s | ... | 关键数字 | 稍慢 |
| 金句 | 20-25s | ... | 全屏金句 | 慢 |
| 结尾 | 25-30s | ... | ... | 正常 |

**封面建议**：<一句话描述封面画面 + 文字叠层>
**双平台标签**：
- 视频号：#xxx #xxx
- 小红书：#xxx #xxx
```

### 8.7 自动剪辑（新增）

脚本和拍摄都完成后，用户说"剪这条"即可触发 `video-editor` 技能（详见 `.claude/skills/video-editor.md`）。

**四种模式**：
| 模式 | 使用场景 | 命令 |
|------|----------|------|
| 全自动剪辑 | 脚本+素材都有，直接出片 | `python video_editor.py <脚本> <素材>` |
| 对齐检查 | 素材质量不确定，先看对齐 | 加 `--align-only` |
| 剪辑指令 | 还没拍，先生成指令 | 加 `--dry-run` |
| 风格复刻 | 模仿参考视频的剪辑节奏 | 用户提供参考视频链接，Claude 逐秒拆解 |

**核心能力**：Whisper 转录口播素材 → 与脚本自动对齐 → FFmpeg 自动裁剪/调速/字幕/画面放大 → 输出成片。详见 `video_editor.py`。

## 反馈录入

用户发布后把数据告诉你，执行：

1. 更新 `data/history.json` 中对应日期的 `performance` 字段
2. **新增**：如果用户告知有客户通过某条视频来咨询，记录到 `memory/layer4-feedback/client-inquiry-log.md`
3. 当积累 ≥3 条已发布数据后，跑 `python engine.py analyze` 看洞察

## 每周复盘

每 5-7 天跑一次 `python engine.py analyze`，读 `data/insights.json`，告诉用户：
- 哪种策略互动率最高
- 哪种时长完播率最好
- 趋势在往上还是往下
- 下周策略建议

**每4周深度复盘**：在每周复盘基础上，额外做以下检查：
- 读 `memory/layer1-identity/positioning.md`：定位是否需要修正？
- 读 `memory/layer4-feedback/client-inquiry-log.md`：哪种视频带来的客户质量最高？
- 读 `memory/layer2-strategy/competitor-tracker.md`：赛道有没有值得关注的变化？
- 将复盘结论写入 `memory/layer2-strategy/phase-review.md`
- 判断是否需要阶段切换（验证期→深耕期→破圈期）

## 写脚本的核心原则

1. **中心思想先行**：没定论点不下笔。每段只论证这一件事。
2. **具体 > 泛泛**：讲"3700 万 vs 7830 万差在哪"而不是"买房要仔细对比"
3. **一条素材一个角度**：同一个素材面对不同观众应该讲不同故事
4. **诚实比煽动高级**：敢说缺点，承认不完美。高净值人群最反感被忽悠
5. **视频内只引导互动，不引导成交**：CTA 是点赞/关注/收藏，不是"找我分析""帮你看看"。转化靠内容价值自然发生，不是靠视频里喊话。详见 `memory/layer2-strategy/video-cta-strategy.md`

## 平台合规

小红书/视频号发布前检查：
- 敏感词替换：投资→资产配置，升值→保值，抄底→入手时机
- 绝对化用语软化：千万别买→建议谨慎考虑
- 引流话术替换：私信我/加微信→评论区交流。注意：引导关注/点赞可以做，这不属于引流敏感词

## 常用命令速查

```bash
python engine.py status                  # 今日策略/支柱/格式推荐
python engine.py record <脚本路径>       # 记录脚本到 history.json
python engine.py analyze                 # 分析已发布数据，生成 insights
python viral_validator.py <脚本路径>     # 9 维爆款验证（27 分制）
python pleasure_scorer.py <脚本路径>     # 爽点评分（12 分制）
python douyin_extractor.py <链接> --asr --comments  # 提取抖音视频口播文案+评论
python video_editor.py <脚本路径> <素材路径>          # 全自动剪辑（对齐+渲染）
python video_editor.py <脚本路径> <素材路径> --align-only  # 只对齐检查，不渲染
python video_editor.py <脚本路径> <素材路径> --dry-run     # 只生成剪辑指令
```
